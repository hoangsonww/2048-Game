<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        The game itself never uses the network: a player who never signs in
        makes no request. This permission exists solely for the optional
        account, cross-device sync, and leaderboard surface described in
        docs/backend.md, and it is the only permission the app declares.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Game2048">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
